#include <bits/stdc++.h>
using namespace std;

void solution() {
    long long a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >>f;
    cout << a+b << "\n";
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    cin >> t;
    while (t--) {
        solution();
    }
}
