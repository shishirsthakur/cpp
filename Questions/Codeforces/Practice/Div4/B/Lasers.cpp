#include <bits/stdc++.h>
using namespace std;

void solution() {
    long long n, m, x, y;
    cin >> n >> m >> x >> y;
    for(int i = 0; i < n; i++){
        long long a;
        cin >> a;
    }  
    for(int i = 0; i < m; i++){
        long long b;
        cin >> b;
    }
     
    cout << n+m << "\n";
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t;
    cin >> t;
    while (t--) {
        solution();
    }
}
