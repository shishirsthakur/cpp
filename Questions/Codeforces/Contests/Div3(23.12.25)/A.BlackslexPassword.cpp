    #include <bits/stdc++.h>
    using namespace std;
     
    void solution() {
        long long k, x;
        cin >> k >> x;
        long long soln = k*x+1;
        cout << soln << "\n";
    }
     
    int main() {
        ios::sync_with_stdio(0);
        cin.tie(0);
        
        int t;
        cin >> t;
        while (t--) {
            solution();
        }
    }